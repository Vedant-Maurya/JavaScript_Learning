const names = "Ram"
const repoCount = 15

//console.log(name + repoCount + "Value");

console.log(`hello my name is ${names} and my repocount is ${repoCount}`);

const gameName = new String('Ram')  // important sytax that why i used the console in the browser

// console.log(gameName[0]);
// console.log(gameName.length);

// console.log(gameName.toUpperCase()) // used to uppercase in the string
// console.log(gameName.charAt(2))      // check the position of character using the index number
// console.log(gameName.indexOf('R'))   // check the postion of character using 


// console.log(gameName.toLowerCase())  // its used to lower case in string

// const newString = gameName.substring(0,4)   // Slicing of the String using this syntax
// console.log(newString)

// const anotherString = gameName.slice(-2, 1)  // its used the minus value like that negative values used 
// console.log(anotherString);

// const newStringOne = "  RamSita  " 
// console.log(newStringOne)
// console.log(newStringOne.trim())   // (trim) is basically used to remove the white spaces 


const url = "https://vedant.com/vedant%20website"
console.log(url.replace('%20', '-')) // (replace) its basically used to replace the any words (what replace, then what write )

console.log(url.includes('vedant'))   // (includes) its basically used to check the what number in there string yes and no
console.log(url.includes('ram'))     //

console.log(url.split('//'))        // (split) its basically used to search some character in my string it used 


